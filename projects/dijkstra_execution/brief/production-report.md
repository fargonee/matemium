# Production report — Dijkstra Execution

## Outcome

- **Project behavior completed:** A mute execution trace covering
  initialization, minimum selection, accepted/rejected relaxation, repeated
  settlement, predecessor reconstruction, and the negative-edge boundary.
- **Engine behavior changed:** None during this flagship reauthoring.
- **Project-local helpers added:** Heap-based event snapshots, logical-frontier
  extraction, stable execution-board diagrams, and a deterministic
  counterexample.
- **Authoring check:** `check_project` passed with 37 authored timeline items,
  no errors, and no warnings.
- **Accepted preview:** 960×540, 15 fps, landscape, 56.730078 seconds,
  2,288,331 bytes, and 64 rendered animations.
- **Mute verification:** `ffprobe` reported one H.264 video stream and no audio
  stream.

## Visual repair record

1. The first preview established that the dense execution board remained
   readable at 960×540.
2. Full-resolution frames exposed an overflowing initialization sentence and
   collapsed boundaries in a mixed-rich-run flex comparison.
3. Initialization was reduced to a symbolic three-part statement.
4. The flex cards were replaced by two centered single-run verdict lines:
   green UPDATE and red KEEP.
5. The accepted contact sheet and representative frames show the registered
   graph/state board, full relaxation cycle, accelerated frontier, highlighted
   final path, negative-edge counterexample, and isolated invariant legibly.

## Algorithm checks

- 25 generated events and deterministic alphabetical tie-breaking.
- Final distance map and predecessor tree match the verified heap run.
- Reconstructed route cost is 13.
- Negative-edge counterexample costs are internally consistent.
- Source-and-execution review is recorded in `brief/domain-review.md`;
  independent expert sign-off remains pending.

## Honest readiness

The authoring-stage preview is accepted and the project is a bundled flagship
candidate. Final public delivery still requires a visually inspected
1920×1080 master and independent algorithms sign-off. The board uses staged
registered snapshots, not an advertised reactive multi-widget trace cursor.
